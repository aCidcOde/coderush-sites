# CodeRush Hub (raiz)
server {
    listen 80;
    server_name coderush.com.br www.coderush.com.br localhost;
    root /var/www/html;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}

# Sistema Venda Direta
server {
    listen 80;
    server_name sistemavendadireta.com.br www.sistemavendadireta.com.br;
    root /var/www/html/sistemavendadireta;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}

# Codafacil
server {
    listen 80;
    server_name codafacil.dev www.codafacil.dev;
    root /var/www/html/codafacil;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}

# WordPress Consultoria
server {
    listen 80;
    server_name wordpressconsultoria.com.br www.wordpressconsultoria.com.br;
    root /var/www/html/wordpressconsultoria;
    index index.html index.php;

    location / {
        try_files $uri $uri/ =404;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}

# FluxoInteligente IA
server {
    listen 80;
    server_name fluxointeligenteia.com.br www.fluxointeligenteia.com.br;
    root /var/www/html/fluxointeligenteia;
    index index.html index.php;

    location / {
        try_files $uri $uri/ =404;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}
